package test.liuzehua.securitydemo.config;import org.springframework.security.crypto.password.PasswordEncoder;import org.springframework.stereotype.Component;/** * NoOpPasswordEncoder 过时 * 自定义明文密码的校验 */@Componentpublic class JWTPasswordEncoder implements PasswordEncoder {    private static  final JWTPasswordEncoder  INSTANCE = new JWTPasswordEncoder();    @Override    public String encode(CharSequence charSequence) {        //不做任何加密处理        return charSequence.toString();    }    @Override    public boolean matches(CharSequence charSequence, String s) {        //charSequence是前端传过来的密码，s是数据库中查到的密码       return charSequence.toString().equals(s);    }    public static PasswordEncoder getInstance() {        return INSTANCE;    }    private JWTPasswordEncoder() {    }}